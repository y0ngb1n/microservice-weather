server:
  port: 8084

# 日志
logging:
  level:
    per.study.*: debug

spring:
  application:
    name: micro-weather-city-server

eureka:
  client:
    service-url:
      default-zone: http://weather-eureka:8761/eureka/